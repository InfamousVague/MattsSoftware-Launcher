{
  "$schema": "../gen/schemas/capability.json",
  "identifier": "default",
  "description": "Default permissions for the MattsSoftware launcher",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
